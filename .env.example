# KnowledgeDB Environment Configuration

# ==============================================
# SERVER CONFIGURATION
# ==============================================

# Server port (default: 5000)
PORT=5000

# Node environment (development | production | test)
NODE_ENV=development

# ==============================================
# SECURITY & AUTHENTICATION
# ==============================================

# JWT Secret - MUST be changed in production!
# Generate a strong 256-bit secret: openssl rand -base64 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=CHANGE-THIS-TO-A-STRONG-256-BIT-SECRET-IN-PRODUCTION

# JWT Token Expiry (default: 7d)
# Examples: 1h, 24h, 7d, 30d
JWT_EXPIRY=7d

# Bcrypt Salt Rounds (default: 12, higher = more secure but slower)
# Recommended: 12-14 for production
BCRYPT_ROUNDS=12

# ==============================================
# ADMIN ACCOUNT
# ==============================================

# Admin email for initial setup
ADMIN_EMAIL=admin@example.com

# Admin password - MUST be changed in production!
ADMIN_PASSWORD=CHANGE-THIS-ADMIN-PASSWORD

# ==============================================
# RATE LIMITING
# ==============================================

# Default rate limit (requests per minute)
RATE_LIMIT_PER_MINUTE=100

# Auth rate limit (requests per 15 minutes)
AUTH_RATE_LIMIT=20

# Heavy operations rate limit (requests per minute)
HEAVY_RATE_LIMIT=20

# ==============================================
# MEMORY SYSTEM
# ==============================================

# Maximum memory items per agent (default: 1000)
MAX_MEMORY_ITEMS_PER_AGENT=1000

# ==============================================
# EMAIL CONFIGURATION (Optional - for password reset)
# ==============================================

# SMTP Server
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Email sender information
# EMAIL_FROM=noreply@knowledgedb.com
# EMAIL_FROM_NAME=KnowledgeDB

# ==============================================
# CORS CONFIGURATION (Optional)
# ==============================================

# Comma-separated list of allowed origins
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# ==============================================
# DATABASE CONFIGURATION (Optional - future expansion)
# ==============================================

# Currently using file-based storage
# Uncomment if migrating to external database

# DATABASE_TYPE=postgresql
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=knowledgedb
# DATABASE_USER=db_user
# DATABASE_PASSWORD=db_password

# ==============================================
# LOGGING & MONITORING (Optional)
# ==============================================

# Log level (error | warn | info | debug)
# LOG_LEVEL=info

# Log file path (leave empty to disable file logging)
# LOG_FILE=./logs/knowledgedb.log

# ==============================================
# EXTERNAL SERVICES (Optional)
# ==============================================

# Webhook signature secret (for HMAC verification)
# WEBHOOK_SECRET=your-webhook-secret-key

# API rate limiting storage (memory | redis)
# RATE_LIMIT_STORE=memory

# Redis URL (if using Redis for rate limiting or caching)
# REDIS_URL=redis://localhost:6379

# ==============================================
# FRONTEND CONFIGURATION
# ==============================================

# Frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:3000

# ==============================================
# DEPLOYMENT (Production)
# ==============================================

# Public URL of your deployment
# PUBLIC_URL=https://your-knowledgedb-domain.com

# SSL/TLS Certificate paths (if using HTTPS)
# SSL_KEY=/path/to/privkey.pem
# SSL_CERT=/path/to/fullchain.pem

# ==============================================
# DEVELOPMENT ONLY
# ==============================================

# Enable debug mode (verbose logging)
# DEBUG=knowledgedb:*

# Disable authentication (NEVER use in production!)
# DISABLE_AUTH=false

# ==============================================
# NOTES
# ==============================================

# 1. Copy this file to .env in the project root
# 2. Update all "CHANGE-THIS" values
# 3. Never commit .env to version control
# 4. Generate strong secrets using:
#    - openssl rand -base64 32
#    - node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# 5. Use different values for development and production environments
# 6. Keep your .env file secure and backed up separately

